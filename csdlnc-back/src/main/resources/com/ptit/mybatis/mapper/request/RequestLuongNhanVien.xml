<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ptit.csdlnc.dao.request.RequestLuongNhanVienDAO">

 
   <select id="getLuongNhanVien" parameterType="map" resultType="map">
    SELECT
        nv.ma_nhan_vien,
        nv.ho_ten,

        <!-- /* ===== LƯƠNG CỨNG ===== */ -->
        bl.muc_luong_co_ban AS luong_co_ban,

        <!-- /* ===== LƯƠNG TRÁCH NHIỆM (chỉ quản lý) ===== */ -->
        CASE
            WHEN cv.ten_chuc_vu IN ('TruongPhong','PhoPhong')
            THEN COALESCE(SUM(DISTINCT da.luong_trach_nhiem),0)
            ELSE 0
        END AS luong_trach_nhiem,

        <!-- /* ===== LƯƠNG NĂNG SUẤT ===== */ -->
        (
            bl.muc_luong_co_ban *
            (
                0.1 * COUNT(DISTINCT tgda.ma_du_an)
                +
                0.05 * COUNT(DISTINCT CASE
                        WHEN tgda.vai_tro = 'ChuTri'
                        THEN tgda.ma_du_an
                END)
            )
            +
            COALESCE(SUM(lcv.muc_luong_nang_suat),0)
        ) AS luong_nang_suat,

        <!-- /* ===== SỐ PHẦN VIỆC BỊ PHẠT ===== */ -->
        (
            COUNT(DISTINCT CASE
                WHEN cd.ket_qua = 'KEM'
                OR (cd.ngay_hoan_thanh_thuc_te &gt; cd.ngay_hoan_thanh_du_kien)
                THEN CONCAT(cd.ma_du_an,'-',cd.stt_cong_doan)
            END)
            +
            COUNT(DISTINCT CASE
                WHEN cviec.ket_qua = 'KEM'
                OR (cviec.ngay_hoan_thanh_thuc_te &gt; cviec.ngay_hoan_thanh_du_kien)
                THEN cviec.ma_cong_viec
            END)
        ) AS so_phan_viec_loi,

        <!-- /* ===== TỔNG LƯƠNG ===== */ -->
        (
            bl.muc_luong_co_ban
            +
            CASE
                WHEN cv.ten_chuc_vu IN ('TruongPhong','PhoPhong')
                THEN COALESCE(SUM(DISTINCT da.luong_trach_nhiem),0)
                ELSE 0
            END
            +
            (
                bl.muc_luong_co_ban *
                (
                    0.1 * COUNT(DISTINCT tgda.ma_du_an)
                    +
                    0.05 * COUNT(DISTINCT CASE
                            WHEN tgda.vai_tro='ChuTri'
                            THEN tgda.ma_du_an
                    END)
                )
                +
                COALESCE(SUM(lcv.muc_luong_nang_suat),0)
            )
        ) AS tong_luong,

        <!-- /* ===== TIỀN PHẠT ===== */ -->
        (
            0.08 *
            (
                bl.muc_luong_co_ban
                +
                CASE
                    WHEN cv.ten_chuc_vu IN ('TruongPhong','PhoPhong')
                    THEN COALESCE(SUM(DISTINCT da.luong_trach_nhiem),0)
                    ELSE 0
                END
                +
                (
                    bl.muc_luong_co_ban *
                    (
                        0.1 * COUNT(DISTINCT tgda.ma_du_an)
                        +
                        0.05 * COUNT(DISTINCT CASE
                                WHEN tgda.vai_tro='ChuTri'
                                THEN tgda.ma_du_an
                        END)
                    )
                    +
                    COALESCE(SUM(lcv.muc_luong_nang_suat),0)
                )
            )
            *
            (
                COUNT(DISTINCT CASE
                    WHEN cd.ket_qua = 'KEM'
                    OR (cd.ngay_hoan_thanh_thuc_te &gt; cd.ngay_hoan_thanh_du_kien)
                    THEN CONCAT(cd.ma_du_an,'-',cd.stt_cong_doan)
                END)
                +
                COUNT(DISTINCT CASE
                    WHEN cviec.ket_qua = 'KEM'
                    OR (cviec.ngay_hoan_thanh_thuc_te &gt; cviec.ngay_hoan_thanh_du_kien)
                    THEN cviec.ma_cong_viec
                END)
            )
        ) AS tien_phat

    FROM nhan_vien nv

    LEFT JOIN xep_bac_luong xbl
        ON nv.ma_nhan_vien = xbl.ma_nhan_vien

    LEFT JOIN bac_luong bl
        ON bl.ma_bac_luong = xbl.ma_bac_luong

    LEFT JOIN chuc_vu cv
        ON nv.ma_nhan_vien = cv.ma_nhan_vien

    LEFT JOIN tham_gia_du_an tgda
        ON nv.ma_nhan_vien = tgda.ma_nhan_vien
        AND tgda.thang = #{thang}
        AND tgda.nam = #{nam}

    LEFT JOIN du_an da
        ON tgda.ma_du_an = da.ma_du_an

    LEFT JOIN thuc_hien_cong_doan thcd
        ON nv.ma_nhan_vien = thcd.ma_nhan_vien

    LEFT JOIN cong_doan cd
        ON cd.stt_cong_doan = thcd.stt_cong_doan
        AND cd.ma_du_an = thcd.ma_du_an
        AND cd.ngay_bat_dau &lt;= LAST_DAY(STR_TO_DATE(CONCAT(#{nam},'-',#{thang},'-01'),'%Y-%m-%d'))
        AND cd.ngay_hoan_thanh_du_kien
            &gt;= STR_TO_DATE(CONCAT(#{nam},'-',#{thang},'-01'),'%Y-%m-%d')

    LEFT JOIN thuc_hien_cong_viec thcv
        ON nv.ma_nhan_vien = thcv.ma_nhan_vien

    LEFT JOIN cong_viec cviec
        ON cviec.ma_cong_viec = thcv.ma_cong_viec
        AND cviec.ngay_bat_dau &lt;= LAST_DAY(STR_TO_DATE(CONCAT(#{nam},'-',#{thang},'-01'),'%Y-%m-%d'))
        AND cviec.ngay_hoan_thanh_du_kien
            &gt;= STR_TO_DATE(CONCAT(#{nam},'-',#{thang},'-01'),'%Y-%m-%d')

    LEFT JOIN loai_cong_viec lcv
        ON lcv.ma_loai_cv = cviec.ma_loai_cv
    WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND (
                nv.ho_ten LIKE CONCAT('%', #{keyword}, '%')
                OR nv.ma_nhan_vien LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
    GROUP BY
        nv.ma_nhan_vien,
        nv.ho_ten,
        bl.muc_luong_co_ban,
        cv.ten_chuc_vu

    <if test="sortField != null and sortField != ''">
        ORDER BY
        <choose>
            <when test="sortField == 'tong_luong'">
                tong_luong
            </when>
            <when test="sortField == 'so_phan_viec_loi'">
                so_phan_viec_loi
            </when>
            <otherwise>
                ORDER BY tong_luong DESC
            </otherwise>
        </choose>

        <choose>
            <when test="sortOrder == 'asc'"> ASC </when>
            <otherwise> DESC </otherwise>
        </choose>
    </if>

    </select>

   <select id="countNhanVien" parameterType="map" resultType="int">
        SELECT count(*)
        FROM nhan_vien
        WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND (
                ho_ten LIKE CONCAT('%', #{keyword}, '%')
                OR ma_nhan_vien LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
    </select>


</mapper>